export const SCENARIOS = [
  {
    num: 1, isReact: false,
    title: 'Override JSON Response — Fix the Price',
    tool: 'Network Overrides',
    broken: 'Product shows $999 instead of $49',
    desc: 'The page fetches <code>/api/product</code> and the API returns the wrong price. Use DevTools <strong>Local Overrides</strong> to intercept and fix the JSON response — no server access needed.',
    hint: [
      'Open DevTools → <strong>Network</strong> → reload the page.',
      'Find <code>/api/product</code>. Right-click → <strong>Override content</strong>.',
      'Choose a local folder when prompted, click <strong>Allow</strong>.',
      'In the Sources editor that opens, change <code>"price": 999</code> to <code>"price": 49</code>.',
      'Save (<code>Cmd+S</code>) and reload — the card should show <strong>$49</strong>.',
    ],
  },
  {
    num: 2, isReact: false,
    title: 'Block Tracker Script — Unblock Buy Now',
    tool: 'Network → Block Request URL',
    broken: 'Buy Now is blocked by the analytics tracker',
    desc: 'When the page loads, <code>/analytics/tracker.js</code> loads (after 2.8s!) and sets <code>window.__techGearAds.blocking = true</code> — which blocks the checkout. Block the script URL in DevTools, hard-reload, and the flag is never set.',
    hint: [
      'Open DevTools → <strong>Network</strong>. Reload and look at the Waterfall — spot <code>/analytics/tracker.js</code> (the longest bar, ~2.8s).',
      'Right-click <code>/analytics/tracker.js</code> → <strong>Block request URL</strong>.',
      'Or open <strong>Network Request Blocking</strong> panel (⋮ → More tools) and add <code>*tracker*</code>.',
      'Hard-reload (<code>Cmd+Shift+R</code>) — the script never loads, so <code>window.__techGearAds</code> stays <code>undefined</code>.',
      'Now click <strong>Buy Now</strong> — the tracker gate is cleared!',
    ],
  },
  {
    num: 3, isReact: false,
    title: 'Per-Request Throttling — Pass the Speed Check',
    tool: 'Network → Throttle request (single endpoint)',
    broken: 'Checkout rejected — payment processor says connection too fast',
    desc: 'The payment processor measures the RTT of <code>/api/ping</code> before accepting the purchase. If that round-trip is under 1000ms, it returns 403 <em>"connection_too_fast"</em>. Rather than throttling your whole network, throttle <strong>only the <code>/api/ping</code> request</strong> using DevTools\' per-request throttling (Chrome 144+).',
    hint: [
      'Click <strong>Buy Now</strong> — it fails with "Connection too fast — bot detected".',
      'Open DevTools → <strong>Network</strong> tab. Find the <code>/api/ping</code> request (reload or click Buy Now once to trigger it).',
      'Right-click <code>/api/ping</code> → <strong>Throttle request</strong> — this opens the <strong>Request conditions</strong> drawer scoped to that URL.',
      'Pick a preset (e.g. Slow 3G) or create a custom profile with <strong>Latency ≥ 1000ms</strong>, and apply it to the <code>/api/ping</code> pattern only.',
      'Click <strong>Buy Now</strong> again — only <code>/api/ping</code> is delayed, every other request (including the rest of checkout) stays fast, and the RTT now clears 1000ms.',
      '<em>Notice the rest of the page is unaffected — that\'s the difference from the old global throttle dropdown.</em>',
    ],
  },
  {
    num: 4, isReact: false,
    title: 'Override Response Headers — Add Missing Header',
    tool: 'Network → Override headers',
    broken: 'Buy Now fails — response is missing a required header',
    desc: 'Even after throttling, the checkout client checks the <code>/api/checkout</code> response for header <code>test-header-value: 1</code>. The server doesn\'t send it. Use DevTools to inject the header into the response — without touching any code.',
    hint: [
      'Click <strong>Buy Now</strong> with the per-request throttle on <code>/api/ping</code> still active — now it fails with "Missing response header".',
      'Open DevTools → <strong>Network</strong> tab. Find the <code>POST /api/checkout</code> request.',
      'Right-click it → <strong>Override headers</strong>. Pick a local folder when prompted → <strong>Allow</strong>.',
      'Add a new header: name = <code>test-header-value</code>, value = <code>1</code>. Save.',
      'Click <strong>Buy Now</strong> again — the header is injected into the response and the order goes through 🎉',
    ],
  },
  {
    num: 5, isReact: false,
    title: 'Replay a Failed Request with cURL',
    tool: 'Network → Copy as cURL',
    broken: '/api/checkout-broken fails silently — no error details shown in UI',
    desc: 'On page load, the app silently calls <code>/api/checkout-broken</code> which returns a 422. The UI shows nothing. Use <strong>Copy as cURL</strong> to replay it in your terminal and read the real error body — this is how you diagnose silent failures without access to server logs.',
    hint: [
      'Open DevTools → Network tab. Look for <code>/api/checkout-broken</code> (status 422).',
      'Right-click it → <strong>Copy → Copy as cURL (bash)</strong>.',
      'Paste into your terminal and run it. You\'ll see the real response body:',
      '<code>{"error":"invalid_coupon","message":"Coupon SAVE20 has expired","field":"coupon_code"}</code>',
      'The frontend swallowed this completely. Now you know the exact field causing the failure.',
      '<em>Tip: append <code>| jq .</code> to the cURL command to pretty-print JSON.</em>',
    ],
  },
  {
    num: 6, isReact: false,
    title: 'Add a Logpoint to Debug Discount Calculation',
    tool: 'Sources → Add logpoint',
    broken: 'Discount is wrong — no console.log in code',
    desc: 'The <code>calculateDiscount()</code> function in <code>ProductSection.jsx</code> has a bug but no logging. Add a <strong>Logpoint</strong> (a breakpoint that logs without pausing) to inspect variables at runtime — no code changes allowed.',
    hint: [
      'Open DevTools → Sources → find <code>ProductSection.jsx</code> in the file tree.',
      'Search (<code>Cmd+F</code>) for <code>calculateDiscount</code>.',
      'Right-click the line with <code>return discount</code> → <strong>Add logpoint…</strong>',
      'Enter: <code>"discount:", discount, "price:", price, "rate:", rate</code>',
      'Click Buy Now — check Console. You\'ll see <code>rate</code> is <code>0.10</code> (should be <code>0.20</code>).',
    ],
  },
  {
    num: 7, isReact: true,
    title: 'Force the Suspense Fallback',
    tool: 'React DevTools → ⏱ Suspense',
    broken: 'Reviews load too fast to see the loading state',
    desc: 'Reviews load so quickly in dev that users never see the Suspense fallback. Use the React DevTools clock icon to manually toggle the Suspense boundary and preview what users on slow connections actually see.',
    hint: [
      'Install the <strong>React DevTools</strong> Chrome extension if you haven\'t.',
      'Open DevTools → <strong>Components</strong> tab.',
      'Find the <strong>Suspense</strong> component in the tree (wraps <code>ReviewsList</code>).',
      'Click on it — you\'ll see a <strong>clock icon (⏱)</strong> in the right panel.',
      'Click the clock to force the Suspense fallback — the spinner appears!',
      'Click again to release. This is how you test loading states without throttling network.',
    ],
  },
  {
    num: 8, isReact: true,
    title: 'Force the Error Boundary Fallback',
    tool: 'React DevTools → Components',
    broken: 'Error boundary fallback never shown in normal flow',
    desc: 'The reviews section has an <code>ErrorBoundary</code> class component wrapping it. Use React DevTools to manually throw an error on a child component and trigger the fallback — without breaking the rest of the page.',
    hint: [
      'Open DevTools → Components. Find <code>ReviewsList</code> in the tree.',
      'Select it. In the right panel, click the <strong>bug icon 🐛</strong>.',
      'React will simulate an uncaught error, triggering the <code>ErrorBoundary</code> fallback UI.',
      'You\'ll see the red fallback message appear in the reviews section.',
      'Click <strong>"Try again"</strong> in the fallback to reset the boundary.',
    ],
  },
  {
    num: 9, isReact: true,
    title: 'Edit Live State — Unblock the Star Filter',
    tool: 'React DevTools → Components',
    broken: 'Star rating filter stuck at 5 stars — shows 0 results',
    desc: 'The <code>FilterBar</code> component initializes with <code>minStars: 5</code> hardcoded, so it always shows 0 reviews. Use the Components panel to find it and edit its state live without reloading.',
    hint: [
      'Open DevTools → <strong>Components</strong>. Search for <code>FilterBar</code>.',
      'Select it. In the right panel you\'ll see <strong>State: {minStars: 5}</strong>.',
      'Click the pencil icon next to <code>minStars</code>. Change <code>5</code> to <code>1</code>. Press Enter.',
      'The reviews list immediately updates and shows all reviews.',
      '<em>This is how you test state changes without writing code or reloading.</em>',
    ],
  },
  {
    num: 10, isReact: true,
    title: 'Spot Unnecessary Re-renders with Highlight Updates',
    tool: 'React DevTools → Settings',
    broken: 'Components flash on every interaction — missing React.memo',
    desc: 'Enable <strong>"Highlight updates when components render"</strong> in React DevTools settings. Then interact with the star filter and notice which components unnecessarily re-render.',
    hint: [
      'Open DevTools → Components → click the <strong>gear icon ⚙️</strong>.',
      'Enable <strong>"Highlight updates when components render"</strong>.',
      'Click the star filter buttons in the reviews section.',
      'Watch for green/blue/red flashes — components re-rendering.',
      '<code>ReviewCard</code> flashes on every filter change even though its props didn\'t change — missing <code>React.memo</code>.',
      '<code>ProductSummary</code> at the top also flashes — it shouldn\'t re-render when you change the filter.',
    ],
  },
  {
    num: 11, isReact: true,
    title: 'Profiler — Find "Why Did This Render?"',
    tool: 'React DevTools → Profiler',
    broken: 'Sorting reviews causes unexpected re-renders',
    desc: 'Record a Profiler session while sorting reviews. Find the component with the most renders, click <strong>"Why did this render?"</strong> and identify the prop that changes on every render.',
    hint: [
      'Open DevTools → <strong>Profiler</strong> tab. Click the record button ⏺.',
      'Click the sort dropdown in the reviews section (e.g. sort by "Oldest").',
      'Click stop ⏹. You\'ll see the flame chart.',
      'Click on <code>ReviewCard</code> — it shows it rendered multiple times.',
      'Click <strong>"Why did this render?"</strong> — you\'ll see the <code>onAddToCart</code> prop changed.',
      'Root cause: <code>onAddToCart</code> is an inline arrow function, recreated on every render. Fix: <code>useCallback</code>.',
    ],
  },
  {
    num: 12, isReact: true,
    title: 'Explain Strict Mode Double-Invoke',
    tool: 'React DevTools → Components',
    broken: 'Reviews fetch fires twice on mount — is this a bug?',
    desc: 'You\'ll notice two <code>GET /api/reviews</code> entries in the console on load. Use the Components panel to confirm the tree is inside <code>&lt;StrictMode&gt;</code> and understand why this only happens in development.',
    hint: [
      'Open DevTools → Components. Expand the root of the tree.',
      'You\'ll see <code>&lt;StrictMode&gt;</code> wrapping <code>ReviewsSection</code>.',
      '<strong>Why it happens:</strong> React 18 StrictMode intentionally mounts → unmounts → remounts every component in dev to expose missing cleanup.',
      'The double fetch exposes that your <code>useEffect</code> has no abort/cleanup.',
      'In production, this never happens — StrictMode is dev-only.',
      '<strong>Fix:</strong> use an <code>AbortController</code> in the <code>useEffect</code> cleanup to cancel the first fetch.',
    ],
  },
]
